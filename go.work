go 1.25.3

use (
	./rysh-cli-code
	./rysh-cli-shared
)

// rysh-cli-code requires the published rysh-cli-shared module. Inside this
// workspace it resolves to the sibling checkout instead, so the two evolve
// together without a release round-trip.
//
// GENERATED by scripts/publish-oss.sh from scripts/oss-assets/parent-go.work in
// the private repo — do not edit here. The version below is read out of the
// rysh-cli-code tree being published in the same run and asserted to match its
// `require` directive; a mismatch fails the publish. It used to be a comment
// asking a human to keep the two in sync, and the human did not.
replace github.com/rysh-ai/rysh-cli-shared v0.2.5 => ./rysh-cli-shared
